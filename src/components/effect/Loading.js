import React from 'react';
import { Alert, Space, Spin } from 'antd';

const Loading = () => (
    <div className="loading image-item">
        <Spin size="large" />
    </div>
);
export default Loading;
